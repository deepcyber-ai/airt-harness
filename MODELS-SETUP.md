# Configuring and testing your models — moved

> **This guide has moved.** Model configuration and testing now lives in
> **[Configuring & testing your models](configuring-models.html)**
> (`configuring-models.html`), linked from **Getting Started**.

It's the same material — adding a credential, launching a target, checking model
selection, switching models live (GUI / API / target), adding your own model
(including host-side Ollama from the VM), and troubleshooting — kept current with the
harness.

Open `configuring-models.html`, or the markdown source `CONFIGURING-MODELS.md`.
